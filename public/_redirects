# Netlify redirects for MemoryMixer subscription website

# Clean URLs - redirect .html to clean paths
/subscribe.html   /subscribe   301
/success.html     /success     301

# API routes to Netlify Functions
/api/*  /.netlify/functions/:splat  200

# SPA fallback for any unmatched routes
/*    /index.html   200